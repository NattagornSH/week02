use("sample_mflix");
// Fetch all documents from comments collection.
db.comments.find({});

//Fetch only one user from comments collection by _id = ObjectId(“5a9427648b0beebeb69579f5”).
db.comments.findOne({ _id: ObjectId("5a9427648b0beebeb69579f5") })

//Fetch only one user from comments collection by email = “john_bishop@fakegmail.com”.
db.comments.findOne({ email: "john_bishop@fakegmail.com" });

//Fetch only one user from comments collection by name = “John Bishop”.
db.comments.findOne({ name: "John Bishop" });


/*
### **1. มองภาพรวม (The Analogy)**

นึกภาพว่าคุณกำลังทำงานอยู่ในห้องเก็บเอกสาร:

* **Database (`sample_mflix`)**: คือ "ห้องเก็บเอกสาร" ทั้งห้อง
* **Collection (`comments`)**: คือ "ลิ้นชัก" ที่เก็บกระดาษเฉพาะเรื่องคอมเมนต์
* **Document**: คือ "แผ่นกระดาษ" 1 ใบที่มีข้อมูลคน 1 คน

---

### **2. คำสั่งที่เรียนไปคืออะไร?**

| คำสั่ง | เปรียบเทียบกับการทำงานจริง | ผลลัพธ์ที่ได้ |
| --- | --- | --- |
| **`find({})`** | "หยิบกระดาษ **ทุกใบ** ในลิ้นชักออกมาดู" | ได้ปึกกระดาษ (List/Array) |
| **`findOne({_id: ...})`** | "ไปหยิบกระดาษใบที่มี **เลขรหัส (ID)** นี้มาใบเดียว" | ได้กระดาษ 1 แผ่น (Object) |
| **`findOne({email: ...})`** | "ไปค้นหาใบแรกที่เจอว่ามี **Email** นี้เขียนอยู่" | ได้กระดาษ 1 แผ่น (Object) |

---

### **3. จุดที่ต้องระวังเป็นพิเศษ**

* **`ObjectId` คืออะไร?**: เปรียบเหมือน **"บาร์โค้ด"** ครับ เราจะพิมพ์แค่ตัวเลขบาร์โค้ดลงไปดื้อๆ ไม่ได้ (เช่น `5a94...`) เราต้องใช้เครื่องสแกนบาร์โค้ด ซึ่งในที่นี้คือฟังก์ชัน `ObjectId("...")` มาครอบไว้ เพื่อให้ MongoDB รู้ว่านี่คือรหัสประจำตัวนะ ไม่ใช่ข้อความทั่วไป
* **เครื่องหมายคำพูด**: โปรแกรมมิ่งเข้มงวดมากครับ `"` กับ `“` ไม่เหมือนกัน ถ้าคุณก๊อปปี้มาจากแอปจดบันทึก บางทีมันจะเปลี่ยนเป็นแบบโค้ง (Smart Quotes) ซึ่งคอมพิวเตอร์จะอ่านไม่ออก ต้องใช้แบบตรงเท่านั้น

---

### **สรุปวิธีจำง่ายๆ**

1. **`find`** = เอามา **"หลายใบ"** (ใส่ตระกร้ามาให้)
2. **`findOne`** = เอามา **"ใบเดียว"** (ยื่นให้ใส่มือเลย)
3. **`{ }`** = เงื่อนไข (ถ้าว่างคือเอาหมด ถ้ามีค่าข้างในคือการกรอง)
4. **`_id`** = ต้องมี `ObjectId()` ครอบเสมอ

*/